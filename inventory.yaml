---
bigbrother:
  hosts:
    homeserver:
      ansible_port: 2222
      ansible_ssh_private_key_file: ~/.ssh/homeserver
      ansible_become_pass: "{{ root_pass }}"
      ansible_user: tdback
      ansible_host: bigbrother.box
